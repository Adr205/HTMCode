

main(){
    var page: string = "Help";
    var text: string;
    print(page);


    newPage(page);
    newPage("About");
    text = "About";
    newHeader("About", 1, text);


    newHeader(page,1,"HTMCODE");
    newHeader(page,1,"Esta es una guia de uso y de opciones de funciones de HTMCODE");
    newP(page,"HTMCODE es un lenguaje de programacion enfocado en la maquetacion de paginas web");
    newP(page,"Estando enfocado principalmente en la maquetacion de HTML y dejando todo listo para que");
    newP(page,"El usuario solo tenga que preocuparse de la parte de programacion y estilos de la pagina");
    newHeader(page,4,"Todos los resultados se guardan en una carpeta que se crea llamada 'Website'");

    newHeader(page,1,"La funcion primoridial de este programa es newPage(nombre de la pagina) ya que esta es la que crea el directorio de la pagina y su archivo css y js.");
    newHeader(page,2,"Y es por eso que en todas las funciones es necesario poner el nombre de la pagina como primer parametro ya que es a esa pagina donde se le insertaran los cambios.");
    newBr(page);
    newHeader(page,1,"Guia de uso");
    newHeader(page,3,"Recuerda que para todas las funciones siempre se manda como primer parametro el nombre de la pagina a la que se le haran los cambios");
    newHeader(page,2,"nombre_funcion(nombre_pagina y luego los demas parametros necesarios)");
    newHeader(page,2,"Navegacion");
    newP(page,"HTMCODE cuenta con el tag de navegacion que se suele usar en las barras de navegacion y es muy sencillo usarlas.");
    newP(page,"Solo se tiene que llamar la funcion de creacion: startNav y cuando este se vaya a cerrar se llama endNav");
    newHeader(page,2,"Hyperlinks");
    newP(page,"Para crear un hyperlink se usa la funcion newA y se le pasa como parametro el enlace de a donde va a mandar y el texto que mostrara el tag");
    newP(page,"Esta funcion detecta automaticamente si se trata de una pagina del proyecto o un enlace externo y lo muestra de la manera correcta.");
    newP(page,"Este tipo de enlaces muchas veces se usar en la navegacion");
    newP(page,"Un ejemplo de navegacion con hiperenlaces se veria asi:");
    startNav(page);
        text = "Home";
        newA(page, text, text);
        text = "About";
        newA(page, text, text);
        text = "https://www.google.com";
        newA(page, text, "Google");
    endNav(page);
    newP(page,"En este caso solo los enlaces de About y Google mandan a la direccion correcta ya que no existe un archivo de Home en el Proyecto pero si este se creara en un momento despues del proyecto entonces la direccion funcionaria");
    newHeader(page,2,"Headers");
    newHeader(page,3,"Esto es un Header y con la funcion newHeader se puede crear el primer parametro es el nombre de la pagina el segundo es el nivel de header y el tercero es el texto del header puede ser desde el 1 hasta el 5");
    newHeader(page,1,"Esto es un Header 1");
    newHeader(page,2,"Esto es un Header 2");
    newHeader(page,3,"Esto es un Header 3");
    newHeader(page,4,"Esto es un Header 4");
    newHeader(page,5,"Esto es un Header 5");
    newBr(page);
    newHeader(page,2,"Parrafos");
    newP(page,"HTMCODE cuenta con los tipos de parrafos mas usados y solo se necesita el nombre de la pagina el tipo de parrafo y el texto que se escribira");
    newP(page,"Esto es un parrafo normal y se usa con la funcion newP");
    newB(page,"Esto es un parrafo en negrita y se usa con la funcion newB");
    newBr(page);
    newI(page,"Esto es un parrafo en cursiva y se usa con la funcion newI");
    newBr(page);
    newU(page,"Esto es un parrafo subrayado y se usa con la funcion newU");
    newBr(page);
    newLabel(page,"Esto es un label y se usa con la funcion newLabel");
    newBr(page);
    newHeader(page,2,"Imagenes");
    newP(page,"Para insertar una imagen se usa la funcion llamada newImg y se le pasa como parametro el link de la imagen");
    newP(page,"Una foto se mostraria asi:");
    newImg(page, "https://www.google.com/images/branding/googlelogo/2x/googlelogo_color_272x92dp.png");

    newHeader(page,2,"Listas");
    newP(page,"HTMCODE cuenta con los 2 tipos de listas usados en HTML los cuales son UL y OL con su respectivo elemento interno LI y para llamarlos se usa el mismo metodo que con los parrafos" );
    newP(page,"Esto es una lista UL");
    newP(page,"Se llama con la funcion newUl");
    newUl(page);
        newLi(page);
            newP(page,"Esto es un elemento LI de la lista UL");
        endLi(page);
        newLi(page);
            newP(page,"Esto es un elemento LI 2");
        endLi(page);
        newLi(page);
            newP(page,"Esto es un elemento LI 3");
        endLi(page);
    endUl(page);
    newP(page,"La lista UL se cierra con endUl");
    newBr(page);
    newP(page,"Esto es una lista OL");
    newP(page,"Se llama con la funcion newOl");
    newOl(page);
        newLi(page);
            newP(page,"Esto es un elemento LI de la lista OL");
        endLi(page);
        newLi(page);
            newP(page,"Esto es un elemento LI 2");
        endLi(page);
        newLi(page);
            newP(page,"Esto es un elemento LI 3");
        endLi(page);
    endOl(page);
    newP(page,"La lista OL se cierra con endOl");
    newBr(page);
    newHeader(page,2,"Inputs");
    newP(page,"HTMCODE cuenta con todos los tipos de inputs los cuales se llaman con la funcion newInput y como parametros se ingresa el nombre de la pagina y el tipo y algun id." );
    newP(page,"Esto es un input de tipo text");
    text = "text";
    newInput(page,text,"name");
    text = "password";
    newP(page,"Esto es un input de tipo password");
    newInput(page,text,"password");
    text = "email";
    newP(page,"Esto es un input de tipo email");
    newInput(page,text,"email");
    newP(page, "Y tambien se cuenta con los siguientes tipos de inputs");
    newUl(page);
        newLi(page);
            newP(page,"button");
        endLi(page);
        newLi(page);
            newP(page,"checkbox");
        endLi(page);
        newLi(page);
            newP(page,"color");
        endLi(page);
        newLi(page);
            newP(page,"date");
        endLi(page);
        newLi(page);
            newP(page,"datetime-local");
        endLi(page);
        newLi(page);
            newP(page,"file");
        endLi(page);
        newLi(page);
            newP(page,"hidden");
        endLi(page);
        newLi(page);
            newP(page,"image");
        endLi(page);
        newLi(page);
            newP(page,"month");
        endLi(page);
        newLi(page);
            newP(page,"number");
        endLi(page);
        newLi(page);
            newP(page,"radio");
        endLi(page);
        newLi(page);
            newP(page,"range");
        endLi(page);
        newLi(page);
            newP(page,"reset");
        endLi(page);
        newLi(page);
            newP(page,"search");
        endLi(page);
        newLi(page);
            newP(page,"submit");
        endLi(page);
        newLi(page);
            newP(page,"tel");
        endLi(page);
        newLi(page);
            newP(page,"time");
        endLi(page);
        newLi(page);
            newP(page,"url");
        endLi(page);
        newLi(page);
            newP(page,"week");
        endLi(page);
    endUl(page);
    newBr(page);
    newHeader(page,2,"Boton");
    newP(page,"HTMCODE cuenta con una funcion para realizar botones y se llama con la funcion newButton y se le da un parametro para el texto que mostrara.");
    newButton(page,"Enviar");
    newBr(page);
    newHeader(page,2,"Tablas");
    newP(page,"HTMCODE cuenta tambien con el tag de tabla la cual se empieza con la funcion startTable y finaliza con endTable");
    newP(page,"La tabla tambien cuenta con las tags TR TH y TD");
    newP(page,"TR se inicia con startTr y finaliza con endTr");
    newP(page,"TH y TD se llaman con newTh y newTd respectivamente y se les añade el parametro del texto que se mostrara");
    newP(page,"La tabla se ve asi");
    startTable(page);
        startTr(page);
            newTh(page,"Nombre");
            newTh(page,"Apellido");
            newTh(page,"Edad");
        endTr(page);
        startTr(page);
            newTd(page,"Juan");
            newTd(page,"Perez");
            newTd(page,"20");
        endTr(page);
        startTr(page);
            newTd(page,"Maria");
            newTd(page,"Gomez");
            newTd(page,"25");
        endTr(page);
    endTable(page);
    newBr(page);
    newHeader(page,2,"BR");
    newP(page,"HTMCODE cuenta con la funcion newBr para realizar un salto de linea");
    newP(page,"Se usa asi con la funcion newBR");
    newBr(page);
    newP(page,"Y se ve asi como el espacio vacio entre el parrafo y el salto de linea");
    newBr(page);
    newHeader(page,2,"Divisiones");
    newP(page,"HTMCODE cuenta con la funcion newDiv para realizar divisiones");
    newP(page,"Se usa asi con la funcion newDiv y se cierra con endDiv");
    newBr(page);
    newHeader(page,2,"Funciones terminales");
    newP(page,"HTMCODE cuenta con las funciones terminales de secciones que son las siguientes");
    newUl(page);
        newLi(page);
            newP(page,"endBody : que cierra el tag body");
        endLi(page);
        newLi(page);
            newP(page,"endPage : que cierra el tag html");
        endLi(page);
        newLi(page);
            newP(page,"endDiv : que cierra el tag div");
        endLi(page);
    endUl(page);
    newBr(page);
    newHeader(page,2,"Formularios");
    newP(page,"HTMCODE cuenta con la funcion startForm para realizar formularios y endForm");
    newP(page,"Y juntandose con los tags de label button e input se verian asi");
    newP(page,"Login Form");
    startForm(page);
        newLabel(page,"Username");
        text = "text";
        newInput(page,text,"user");
        newLabel(page,"Password");
        text = "password";
        newInput(page,text,"passwords");
        newButton(page,"Login");
    endForm(page);
    newP(page,"Login Form usando divs en cada input");
    startForm(page);
        startDiv(page);
            newLabel(page,"Username");
            text = "text";
            newInput(page,text,"usr");
        endDiv(page);
        startDiv(page);
            newLabel(page,"Password");
            text = "password";
            newInput(page,text,"pass");
        endDiv(page);
        newButton(page,"Login");
    endForm(page);
    newBr(page);
    newHeader(page,3,"Y listo ya solo falta que añadas algunos diseños y programacion y queda lista tu nueva pagina!!");
    newBr(page);
    newLabel(page,"Creado por: ");
    newB(page,"Javier Adrian Villa Leon");
    newP(page,"");
    newLabel(page,"Matricula: ");
    newB(page,"A01242469");
    newBr(page);newBr(page);
    newP(page,"");
    endBody(page);
    endPage(page);
}